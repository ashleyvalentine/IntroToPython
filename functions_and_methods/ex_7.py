# Without running the following code, what do you think it will do?

def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello')

# I think it will throw an error because when the function is invoked on line
# 7 there is only on argument. 